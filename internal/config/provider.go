// Package config provides configuration loading and management functionality.
package config

import "jq-proxy-service/internal/models"

// Provider defines the interface for configuration management
// This is an alias to the ConfigProvider interface in models for better organization
type Provider = models.ConfigProvider
