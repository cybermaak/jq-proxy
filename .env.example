# Server Configuration
# These environment variables override the server settings in the JSON config file
# Endpoints are still loaded from the JSON config file

# Port to listen on (default: 8080)
PROXY_PORT=8088

# Read timeout in seconds (default: 30)
PROXY_READ_TIMEOUT=30

# Write timeout in seconds (default: 30)
PROXY_WRITE_TIMEOUT=30

# Log level for the application
LOG_LEVEL=info